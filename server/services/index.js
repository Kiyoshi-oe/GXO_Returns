// LVS Returns - Services Index
// ============================================

const { startAutoBackup, stopAutoBackup, restartAutoBackup } = require('./autoBackup');

module.exports = {
  startAutoBackup,
  stopAutoBackup,
  restartAutoBackup
};
